function mostrar()
{
	var max // variables
	var min
	var calificacion

	max = 11 // seteo los valores
	min = 0

	//Genero el número RANDOM entre 1 y 10 
	Math.floor(Math.random() * (max - min)) + min;
    
}//FIN DE LA FUNCIÓN